apiVersion: v1
kind: Service
metadata:
  name: user-management-service
  namespace: user-management
  labels:
    app: user-management-app
spec:
  selector:
    app: user-management-app
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  type: ClusterIP